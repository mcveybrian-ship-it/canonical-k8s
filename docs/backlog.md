# Backlog — the single live list of what is outstanding

**This file is the one place to look for "what is left." It is maintained, not archaeological.**

Created 2026-09-18 because the answer to that question was spread across `airgapped-setup-machine/README.md` §0a (priority order, buried in 1,100 lines), `docs/open-questions.md` (the questions, with their history), `docs/compliance/ssp-inputs.md` (the 🔴 statements) and `docs/compliance/baseline/README.md` (which artifacts exist). **None of those is wrong; none of them is a list.**

## How this file works

| | |
|---|---|
| **One home per fact** | An item lives here as a ROW. Its detail, history and evidence stay where they already are, referenced by section or question number. Do not copy the detail in — it will diverge |
| **Updated every session** | When something closes it moves to §9 with a date, it does not vanish. When something new is found it gets a row the same day it is found |
| **Nothing is deleted** | A closed item that turns out not to be closed needs its history. §9 is append-only |
| **Status is honest** | ⬜ not started · 🔄 in progress · ⏸️ deferred *by a named decision* · 🔴 blocked · ✅ done |
| **No secrets** | This file is TRACKED and `origin` is public. Same discipline as the README: no passwords, no keys, no token-bearing paths |

**If you are an assistant reading this: update it as part of the work, not as a separate task afterwards.** A backlog that is maintained at the end of a session is a backlog that is maintained on the sessions that do not run out of room.

---

## 1. 🔴 Red flags — stated in `ssp-inputs.md`, unaddressed

These are the ones an assessor reads first. All four are architectural, not paperwork.

| | Item | Why it matters | Status |
|---|---|---|---|
| **1.1** | ~~**No remote recovery path of any kind**~~ → **now only half a finding** | ✅ **UNLOCK HALF CLOSED 2026-09-21/22: all four hosts boot with no passphrase typed** — host-3 23:36, host-1 23:58, host-2 00:06, **host-4 00:19** (the machine whose prompt took the whole enclave down on 09-17). clevis + TPM 2.0 PCR 7, passphrase retained as fallback. After host-4's reboot the enclave was fully back in **two minutes**: 4/4 guests, DNS, mirror, contracts 200, Harbor healthy, 15/15 targets. 🔴 **The other half stands and is hardware:** nobody can SEE a machine that fails to boot — no console, no power control, no way to tell firmware from GRUB from a failed fsck. **A BMC with serial-over-LAN is the only thing that closes it** (`poam.md` CUST-14, backlog 2.8). ⚠️ And the honest trade, now written for the AO: Secure Boot is OFF, so PCR 7 does not bind the boot chain — **this defeats a stolen disk, not a stolen chassis**, and it is one factor where the passphrase was two (`ssp-inputs.md` §2.1) | 🔄 unlock closed · visibility needs a BMC |
| **1.2** | **Both recovery paths terminate on host-4** | host-4 runs all four service VMs *and* holds the backup drive. It is the single point of failure for the thing meant to survive a failure 🔄 **BACKUP HALF REDUCED 2026-09-20:** `vm-backup.sh second-copy` pushes sets to host-1 after every nightly run — **84 GB, verified byte-for-byte, 877 s**, covering the three guests holding irreplaceable state; the 332 GB mirror is excluded as rebuildable (it alone takes 1 h 11 m to restore). Key restricted to write-only rsync into one directory; RSA 4096 because FIPS refuses ed25519. **The roll-forward half is untouched** — the WAL archive is unbuilt and still aimed at host-4 (`poam.md` ENG-04); design it off host-4 before pg-01..03 exist, while it is free. ⚠️ Both copies are still in one room — §1.3 unaffected | 🔄 backup half done · WAL half open |
| **1.3** | **Survives one host failure, not a site event** | Replica-3 across three hosts cannot self-heal, and every copy is in one room | 🔴 open · §3.1 |
| **1.4** | **The cryptographic-boundary exception has to be won, not assumed** | The FIPS posture is strong and measured (§1.1), but the boundary claim is a negotiation, not a measurement | 🔴 open · §1.3 |

## 2. Big moves — at most one at a time, and they have an order

| | Item | Notes | Status |
|---|---|---|---|
| **2.1** | ~~**Prove a restore works**~~ ✅ **PROVEN 2026-09-20** | `vm-backup.sh restore-test <vm>` built and run: it walks the chain back to its full, verifies every set's checksums, rebuilds into its own directory, defines **restore-test-&lt;vm&gt; with NO network interface** (so it cannot collide with the live guest), boots it, watches a captured serial console for a login prompt, writes evidence, and removes itself. **First run, svc-obs-01 from a 5-set chain (full 09-16 + 4 incrementals): checksums verified, `qemu-img check` passed, BOOTED. Rebuild 39s, boot 17s, total 56s; newest set 14h old.** Evidence: `/srv/stig-evidence/restore-test-svc-obs-01-20260920T213025Z.txt` on host-4 — CP-4 had none of any kind before this. ⚠️ **Found doing it: the backups did not store the domain definition** — only disks. A restore on a rebuilt host would have meant hand-writing the XML under pressure. `virsh dumpxml --inactive` is now saved in every set and checksummed; existing sets predate it and fall back to the live definition WITH A WARNING. ⚠️ **Chain depth grows by one per night (5 today)** — restore time and the number of things that can break grow with it. The full-backup cadence is now a real decision (`BACKUP_KEEP_CHAINS`, the schedule). ✅ **ALL FOUR PROVEN 2026-09-20, written up in `contingency-plan.md` §9.2 (CP-4 test record):** svc-obs-01 56s · svc-harbor-01 1m43s · svc-mgmt-01 3m34s · **svc-repo-01 1h11m** (332 GB; my 25-30 min estimate was wrong - the rebuild reads the chain off the USB volume AND flattens it, ~2x the I/O). All booted, all checksums verified, nothing live touched. **Sequential restore of all four = ~1h20m, dominated by the mirror** - which is rebuildable from the bundle, so the open question is whether it is restored at all. ✅ **RE-TEST DONE 2026-09-21:** the 07:00 set was the first with `DOMAIN.xml`; svc-obs-01 restored from a 6-set chain using **the definition from the backup, not the live domain** — 43 s rebuild, 12 s boot, 55 s total, RPO 8 h. **The backup volume alone now reconstructs a guest.** CP-4 evidence is self-contained (`contingency-plan.md` §9.2) | ✅ CLOSED |
| **2.4** | **TPM unlock for LUKS** | 🔄 **PROVEN ON host-3, 2026-09-21 — it works, and the method is clevis, not systemd-cryptenroll.** host-3 rebooted at 23:36 and **came up with no passphrase typed**; FIPS intact, ufw/exporters/libvirtd/facts all active, and the radio blacklist held. **Why clevis:** `systemd-cryptenroll` is blocked twice over — `libtss2-rc0`, the library systemd *dlopens* for TPM2, **is not in the mirror** (`Candidate: (none)`, the same library that made host-4 report "TPM2 support is not installed" on 09-16), and this stack is `cryptsetup-initramfs`, which has no TPM2 token support at all — which is exactly why host-4's `crypttab` prints `ignoring unknown option tpm2-device`. **clevis, clevis-luks, clevis-tpm2, clevis-initramfs are all in the mirror**, and `tpm2-tools` is the **FIPS build** (5.6-1ubuntu0.0+Fips1). **Measured method:** `tpm2_pcrread sha256:7` returns a value under FIPS · `clevis luks bind -d /dev/sda3 tpm2 '{"pcr_bank":"sha256","pcr_ids":"7"}'` adds **keyslot 1 + a clevis token, leaving keyslot 0 and the passphrase untouched** · sealing key is **ECC** (FIPS-approved) · `update-initramfs -u -k all` then **verify the RUNNING kernel's initrd moved and carries clevis** (11 files; 77 MB → 85 MB). PCR 7 only, per the existing reasoning: PCR 11 changes on every kernel update and would turn "always needs a human" into "needs a human unpredictably at 02:00". ⚠️ **What it does NOT fix:** Secure Boot is off, so PCR 7 does not bind the boot chain — **this defeats a stolen disk, not a stolen chassis**; and it gives no visibility into a FAILED boot, which still needs a BMC (1.1). **Rollback:** `clevis luks unbind -d /dev/sda3 -s 1` + `update-initramfs -u -k all`. ✅ **SCRIPTED AND ROLLED OUT 2026-09-21/22:** `stig-tailor.sh luksenroll` now defaults to `--method=clevis` — it installs the packages, proves the TPM answers under FIPS, binds PCR 7, **removes any inert `tpm2-device=` from crypttab** (so host-4 self-cleans when it runs), rebuilds every kernel's initrd and verifies the running one carries clevis. Idempotent: a second run reports `already bound` and re-verifies without touching a keyslot. It still **refuses without `--force`** when the build recorded `luks_unlock=passphrase`, which is correct — one-factor is a decision, and Q20 is the record of it. **host-1 booted unattended 23:58, host-2 00:06, host-3 23:36** — all three FIPS intact, ufw/exporters/libvirtd/facts active, radios still blocked. **host-1 mattering most: it holds the off-host backup copy**, so after a power event the second copy comes back without a human. ⚠️ Found while scripting it: `rebuild_initramfs` kept **its own inline `lsinitramfs | grep`** and reproduced the same false negative the radio check had — fixing the helper and leaving a copy of the fault fixed nothing. Swept the rest of the enclave and install scripts for unguarded pipeline assignments and inline initrd listings: none left. ✅ **host-4 enrolled 2026-09-22 00:11** — and it revealed a half-applied state: **a clevis binding has existed on its OS volume since 2026-09-16 (keyslot 2) that could never work**, because `clevis-initramfs` was not installed until tonight, so the hook was never in the initrd. The TPM held a key nothing asked it for, and the machine kept prompting — the same shape as the undispatched radio step. Its inert `tpm2-device=auto` is now gone from crypttab (backup `crypttab.20260922T001139`) and the initrd carries 11 clevis files. **Remaining:** host-4 PROOF needs a reboot that stops all four service VMs — use `vm-power.sh` for the ordered shutdown, runbook §10c) · Secure Boot decision (2.8) | 🔄 3 of 4 hosts unattended · host-4 needs a window |
| **2.5** | **Resume the Kubernetes build** — 9 VMs, then K8s, then Ceph | ⏸️ **Paused by decision 2026-09-18.** Nothing half-done. Phase 4 needs only the base image copied and `/etc/enclave/profile` written. ⚠️ Ceph has **never been executed** and wants a dedicated device per OSD this hardware does not have (`ssp-inputs.md` §4.1c) | ⏸️ deferred |
| **2.6** | **From-scratch unattended build of the whole enclave** — the reproducibility proof | Decided 2026-09-19, replacing 2.2 and 2.3. The final install is built from the scripts, inside the gap, unattended — **that build is the proof, not a rebuild of host-4.** It absorbs: host-4's built-before-the-fix history (moot on a fresh machine); **where `svc-repo-01` lives** — decide placement at design time rather than migrate a running mirror (today one host carries DNS/contracts, mirror, Harbor, monitoring and the backup drive, red flags 1.2/1.3); and the host-4 ufw table (3.1) as part of the build, not a retrofit. Until then host-4 is kept, not wiped 🆕 **2026-09-20, Brian: *"in the end we are going to wipe everything and do it from blank machines to make sure the whole process works start to finish"*.** So 2.6 is the **ACCEPTANCE TEST, not a rehearsal**: all four wiped, built from the seed media and the transfer bundle alone, `stage-01` UNPLUGGED, unattended. **The consequence that makes it the real test: the mirror is wiped with everything else, so nothing inside the enclave can be a source.** Anything that exists only on a running machine today is a defect it will find. **Sequence agreed for the rehearsal (2.1 → 2.7 → rebuild host-1/2/3 → evacuate host-4 → rebuild host-4):** each step must prove its inputs come from the repo or the bundle, never from a neighbour. **Before the final wipe, the irreplaceable state must be off the machines and provably restorable** — issuing CA key, Harbor images, Grafana/Prometheus history (2.1 is what makes that claim true)  🆕 **Media prep decided 2026-09-21:** cloud-init NoCloud reads the `CIDATA` volume ROOT only — **subfolders per host do not work** (read from `DataSourceNoCloud.py`; `seedfrom` accepts only `/` and `file://`, and the stick is mounted at a temp path). For this run, build **one stick per host with two partitions** — p1 bootable installer, p2 FAT32 `CIDATA` with that host's seed — so four machines start at once instead of rewriting one stick four times at the rack. Deferred until this prep; host-3 uses the existing two-stick procedure. ⚠️ Every seed holds `LUKS_PASSPHRASE` in plaintext: a stick per host is a credential per host  📍 **host-3 pre-rebuild baseline, captured 2026-09-21 before wiping it — this is what "the rebuild is faithful" will be measured against:** 854 packages (list sha256 `6ace10be1e2f…`), kernel `6.8.0-138-fips` with `fips_enabled=1`, disk layout sha256 `65adf732f68d…`, separate filesystems `/ /home /tmp /var /var/log /var/log/audit /boot` plus both libvirt pools, and esm-apps/esm-infra/fips-updates/usg all enabled. A host built from current scripts should match one built by hand and patched over two weeks; **where it differs, the difference is either a fix we made since or a defect in the scripts**  ✅ **REHEARSAL DONE 2026-09-21 — host-3 rebuilt from blank and it matches.** 855 packages vs host-1's 855, **USG 213/3 on both**, same FIPS kernel, same seven separate filesystems; the only package differences are `wpasupplicant` (on host-1, dead weight with no radio) and `libxml2-utils` (on host-3, today's fix). V1R6: NF=170 Open=5 NR=10 NA=9 — the 5 Open are the smart-card family and audit offload, i.e. the AO decisions, not build defects. **The rebuild found SEVEN defects nothing else had:** `step_radio` never dispatched (so no machine's radio was ever disabled by script) · `sysstat` reinstalled by a fresh build · `libxml2-utils` missing so the tailoring XML was never validated · the `ls`-in-assignment crash that killed hardening silently · the SV-206411 listings change breaking `pro enable` (403 on a directory probe) and `stig-tools fetch` (wget walks the autoindex) · a false initramfs warning that had cried wolf on four hosts since 09-17 · and `push-repo` reporting "cannot ssh" for a changed host key. **Every one of them was invisible to reading the scripts and to the machines already running.** ⚠️ **Owed from it:** consistency pass on host-1/2/4 (`radio disable` for the `/usr/lib` copy, purge `wpasupplicant`, install `libxml2-utils`), collect host-3 evidence, and a rescan so V-270755 leaves Not Reviewed | ⬜ the final acceptance run |
| **2.7** | **Placement is not data — make it a map, a rule set and a planner** | Raised by Brian 2026-09-19/20: *"machine / VM positioning"*, and *"the scripts just knowing where to put things and wire them up"*. **Today placement is implied, not declared:** service VMs land on host-4 because that is where `03-compose-vm.sh` is run (it only WARNS on the wrong host), and pg/k8s placement exists only as comments in `enclave-addresses.env`. That is how red flag 1.2 happened — by accretion, not decision. **Build, in this order:** (a) a placement map + the rules that cannot be measured (etcd, DB data and each Ceph OSD get their own device · the 3 DB guests on 3 different hosts · the backup target is not the host being backed up · RAM reserved per host · N-1: where a host's guests go when it dies), both in `vm-specs.env` beside the sizing; (b) `plan --verify` at install — MEASURES the hosts, checks the map fits, **refuses the build** rather than warning; (c) `plan --spec` before purchase — same rules, no hardware, outputs the BOM requirement. ⚠️ **Do not hand-write hardware facts** (threads/RAM/drives): they are measurable and a stale inventory is worse than none. `01-hw-inventory.sh` already captures them from booted install media before any OS exists — it needs a machine-readable output for the planner. ⚠️ The planner RECOMMENDS and VERIFIES; it never silently reshuffles guests between sites. **Cheap now (8 guests, 4 real), a rewrite after 2.5 (17 guests with anti-affinity that matters)** 🔄 **BUILT 2026-09-20, (a) and (b) done.** `vm-specs.env` now carries `PLACE_<GUEST>` for all 14 guests, `HOST_RESERVE_MB`, `ANTI_AFFINITY`, `PLACE_BACKUP_SECOND`, the dedicated-device rules and a `vm_place()` resolver; the lab profile overrides only the four service VMs. `03-compose-vm.sh` **refuses** to compose a guest away from its declared home (was a warning naming host-4 unconditionally - the mechanism that produced red flag 1.2) and gained `plan` / `plan --spec`. **The planner earned its keep on its first run:** `PLACE_BACKUP_SECOND=host-3` was wrong in BOTH profiles - host-3 runs guests, so their second copy would sit on their own machine. Replaced with a **backup ring** (host-N -> host-N+1, last wraps), which is the only shape that works where every host runs guests. ⚠️ Bug found and fixed in the same session: an unguarded `df` assignment under `set -e` ended the planner silently after the vCPU check **and still exited 0** - the swallowed-failure pattern again. ⚠️ **SECOND SILENT FAILURE, found by running it on host-4 2026-09-20:** `/etc/enclave/profile` is mode 0600 inside a 0700 directory - correctly, because `/etc/enclave/vmbackup.key` (the backup drive's LUKS key) is in there too. So **no non-root run could read the profile and `VM_PROFILE` fell back to `production` silently**: `plan` checked host-4 against a 2-guest layout while it runs 4, and printed *"plan holds"*. Fixed: the declaration moves to `/etc/enclave-build-info` (read first - the installer already writes it per machine) or `/etc/enclave-profile` 0644, the key directory stays closed, and an existing-but-unreadable declaration now prints a loud warning instead of guessing. **Remaining:** write `/etc/enclave-profile` on the four lab hosts; have `02-build-seed.sh` record `vm_profile=` in `/etc/enclave-build-info` so the installation declares itself unattended; (c) machine-readable output from `01-hw-inventory.sh` for pre-OS planning; make `vm-backup.sh` and `vm-power.sh` read the map instead of their own lists | 🔄 map + planner done · consumers left |
| **2.8** | **Production hardware spec — drive count is a chassis decision, RAM is a number** | Decided with Brian 2026-09-20 from measured lab hardware. **Per host ~6 drives: 2 mirrored SSD (OS) · 1 NVMe etcd (every write is an fsync; noisy neighbours cause leader elections) · 1 NVMe DB data · 1–2 NVMe raw per Ceph OSD (Ceph wants a whole device) · 1 NVMe VM pool/images.** ~24 drives across 4 hosts — **a 4-bay chassis makes the design impossible regardless of drive spend. RAM: 128 GiB per host** (production profile is ~56 GiB of guests per host + one service VM + headroom; host-4 already has 125 GiB, hosts 1–3 have 30). ⚠️ Ceph capacity still waits on Q22, and replica-3 means usable = raw/3. Lab measured 2026-09-20: host-1 4t/30G/954G · host-2 4t/30G/932G · host-3 8t/31G/1.8T · host-4 16t/125G/932G+1.9T+1.9T | ⬜ **pre-purchase, feeds 2.7c** |
| **2.9** | **Standard 4-host layout — one service VM per host; LAB CANNOT RUN IT** | Agreed 2026-09-20. **Production standard:** host-1 svc-repo-01 · host-2 svc-harbor-01 · host-3 svc-obs-01 · host-4 svc-mgmt-01 + backup target, each host also carrying a control plane, a worker and a DB guest. Losing any one host then costs one service, not all four + the backups (red flags 1.2/1.3). ⚠️ **Measured 2026-09-20: the lab cannot do it.** The lab cluster profile commits 22 GiB/host, leaving ~8 GiB on hosts 1–3 — svc-mgmt-01 needs 8, svc-obs-01 8, svc-repo-01 16. **No service VM fits beside cluster guests.** So in the lab they stay on host-4 (125 GiB) and the difference between lab and production is ONE LINE IN THE PLACEMENT MAP, not a different procedure. **Lab mitigation for 1.2 instead: second backup copy on host-3** (1.8 TB, most free space); host-4's two spare 1.9 TB NVMe are the right home for the WAL archive and image pool | ⬜ with 2.7 |

## 3. Medium — contained, with a clear finish line

| | Item | Notes | Status |
|---|---|---|---|
| **3.16** | **Lab network rebuild: FortiGate edge, 2.5 G enclave switch, management/storage split** | Decided 2026-09-22 after measuring the network: every host is linked at **1000 Mb**, including host-4 whose RTL8125 is 2.5 GbE-capable and switch-limited. Buying **three USB 2.5 GbE adapters** (host-1/2/3; host-4 needs none) and moving the enclave onto the existing 2.5 G switch lifts host-to-host traffic 2.5x. **The onboard 1 GbE ports then become the MANAGEMENT plane:** keep every current `10.2.20.x` address, DNS record, ufw rule and PPSM entry on the onboard NIC, and add a NEW subnet on the 2.5 G adapters for storage and replication. **Additive, so nothing renumbers.** **Why it is worth the work:** a Ceph rebuild currently competes with SSH and the monitoring scrapes on the same wire — saturate it and you lose access to the machine you are trying to watch; it enables the **Ceph `public_network` / `cluster_network` split**, which cannot be tested at all with one NIC and is a real question for the bake-off; and a dead USB dongle then costs performance, not access. **Cost, stated:** a second interface per host means netplan changes, per-interface ufw rules, updated PPSM/CLSA entries (which service binds to which address), and a decision per listener about binding one address or both — roughly half a day, and it touches compliance documents that are currently clean. **Sequence that avoids pain:** adapters in → confirm `ethtool` shows `r8152` at 2500Mb → run traffic across them for a day → THEN add the second subnet. Not the same evening. ⚠️ Buy RTL8156**B/BG** only (`r8152`, in-kernel, verified present); RTL8157/5 GbE needs an out-of-tree driver and breaks the FIPS configuration  🆕 **Kit confirmed 2026-09-22 and the plan uses only what is owned:** **TL-SG108S-M2** is 8x2.5G **unmanaged** (no VLAN/LACP — not needed), **FG-60F** is 10x**1 GbE** (7 internal, 2 WAN, 1 DMZ — **no 2.5G**, so enclave traffic must never traverse it), GS105E is 1 G. **Target wiring:** FG-60F takes the internet edge (WAN1 current, WAN2 the separate feed) **and the staging segment**, replacing the travel router and the GS105E VLANs · TL-SG108S-M2 is the enclave switch · the umbilical is ONE cable from the TP-Link into a FortiGate internal port, unplugged when the gap is wanted · the GS105E is freed for the management plane or retired. **DO NOT RENUMBER:** give the FortiGate internal interface `10.2.10.1/24` and leave staging and enclave addresses alone — `authorized_keys` carries `from="10.2.10.124"`, and `private-sync`, `push-repo` and `stig-tools` all default to fixed addresses. ⚠️ **Lab vs customer install:** the lab uses a FortiGate port as the break because that is the kit on hand; **the customer design specifies a physical break with no routed path**, since air-gapped and firewall-isolated are different claims to an assessor. One sentence in the runbook so a future reader does not mistake the lab wiring for the design | 🟡 **re-cable DONE 2026-09-22** — FortiGate software switch `lab` (`dmz`+`internal3/4/5`) at `10.2.10.1/24`, travel router and GS105E retired, all 9 machines verified up, gap now = the `dmz` cable. **Starlink moved to `wan2`** (priority 10, so wan1 stays preferred for the office and both IPsec tunnels) and the `lab` segment policy-routed out it — build-01 verified egressing `74.244.117.182` / SpaceX. **`stage-01` moved onto Starlink too** via deny-first policy routes covering all private space — the bare version would have hijacked both IPsec tunnels, since `10.0.20.0/24` is their local selector. ⚠️ **Speed was NOT the win it looked like:** 9 samples put Starlink at 3.8–15.1 MB/s (mean 8.5) against wan1's 7.6–8.8 (mean 8.2) — a wash with four times the variance. Kept for circuit separation, not throughput; revert is one policy delete (runbook §3.1b). Runbook §3.1 rewritten, §3.1a/§3.1b added. **Still open:** the three USB 2.5 GbE adapters and the management/storage NIC split |
| **3.17** | **`build-01` holds a DHCP lease on top of its static address** | Found 2026-09-22 during the FortiGate cable move: `eno1` carries **both** `10.2.10.124/24` (static) and a lease from the `lab` pool, because `dhcp4: true` is still set alongside the static in its netplan. The default route sources from the **leased** address. It works today — on-subnet traffic to `stage-01` sources from `.124` because the connected route wins, which is what keeps `from="10.2.10.124"` in `stage-01`'s `authorized_keys` effective — but a build procedure must not depend on which address a pool hands out. **Fix:** `dhcp4: false` plus an explicit default route via `10.2.10.1` in build-01's netplan, applied with `netplan try` | ⬜ open · small, do before the 2.6 acceptance run |
| **3.15** | **⚠️ A mistyped sudo password locks an admin out until the machine is REBOOTED** (severity corrected 2026-09-22) | Found 2026-09-21 while working on host-3. The hardened policy is `deny=3`, `fail_interval=900`, **`unlock_time=0`** (per DISA: only an administrator clears it), `passwd_tries=1` (so ONE typo = one strike), and **`pam_faillock` is in `common-account` as well as `common-auth` — a locked account is refused SSH PUBKEY logins too**. With **one admin account, no BMC on host-1/2/3 and LUKS at the console**, three typos inside 15 minutes means a physical recovery, not an inconvenience. Got to 2 of 3 strikes today. 🆕 **Written up for the AO 2026-09-22 — `ssp-inputs.md` §2.1b and `poam.md` CUST-15**, with four measures that weaken no control: (1) a second named admin account — `faillock` counts per account, and AC-2 wants individual accountability anyway; (2) **a documented emergency admin account: console-only, credentials offline under dual custody, every session audited by UID, password rotated after each use, each use an incident record** — emergency accounts are contemplated by AC-2 and the compensating controls are physical custody (PE) plus audit (AU-2/AU-12); (3) the **GRUB + LUKS console recovery path written down**, with those credentials in the same custody, ending in `faillock --user <u> --reset`; (4) a **BMC** in the production BOM, which degrades this from architectural to operational. **Named as NOT acceptable so nobody proposes them later:** relaxing `unlock_time`, an automatic `faillock` reset (a brute-force window wearing a convenience label), `NOPASSWD` sudo, a shared admin account, or removing `pam_faillock` from the account stack. **Free mitigation today:** `sudo -i` once per session instead of per-command sudo. **Mitigations to decide (this is a break-glass design question, not a STIG deviation):** a second named admin account (the STIG wants individual accounts anyway, so this costs nothing) · a documented root credential under seal for console recovery · and `faillock --user <u> --reset` as a known step rather than something looked up under pressure. ⚠️ **Do not "fix" this by relaxing `unlock_time`** — that is the control. Fix the single-point-of-failure around it | 🔴 open · decide before the next build |
| **3.1** | **Write host-4's ufw rule table**, then enable (svc-mgmt-01 half ✅ 2026-09-19) | ⏸️ **Deferred by decision 2026-09-18.** NOT a one-liner: both default policies are DROP and host-4's table has two rows with **no `22/tcp`** — enabling as-is drops SSH on a machine with no BMC and cuts bridged traffic for every guest. `svc-mgmt-01`'s MAAS port list is still unconfirmed. 🆕 **2026-09-19: the svc-mgmt-01 half is now easy** — MAAS is gone, and the PPSM generator measured what it actually listens on: 22, 53 tcp+udp (named), 80/443 (nginx), 123/udp (chrony), 9100, plus 8484 (contracts server, meant to be loopback-only — prove with nmap). host-4 is still the hard half | ✅ **svc-mgmt-01 half DONE 2026-09-19** — see §9. **host-4 remains:** it bridges guest traffic, so ufw's FORWARD policy is the risk, not the port list | ⏸️ host-4 deferred |
| **3.2** | **Implement the weekly Trivy DB carry-in** | The AO answered Q27 on 2026-09-18 (weekly). **The process does not exist yet** — the decision is recorded, the mechanism is not built | ⬜ **owed by a decision already made** |
| **3.3** | **Run the V-270754 ufw 443 test** | Engineering half is unrun; the acceptance half is Q25 (deferred). Two owners, one V-ID | ⬜ open |
| **3.4** | **V-270651** AIDE config integrity | Needs a pristine `aide-common` .deb carried in | ⬜ open |
| **3.5** | **V-270747** data-at-rest write-up · ~~V-270719 PPSM~~ → moved to **6a.22** 2026-09-18 (it needs scans and a DISA list, so it is more than paperwork) | Paperwork against mechanisms that already exist | ⬜ open |
| ~~**3.6**~~ | ✅ **CLOSED 2026-09-21 — 8/8 consistent** on `esm-apps`, `esm-infra`, `fips-updates` and `usg`. `esm-infra` + `esm-apps` enabled on host-1/2/3 (host-4 already had both). **The mirror carries all four ESM suites** — verified HTTP 200 on `noble-apps-security/updates` and `noble-infra-security/updates` BEFORE enabling, because enabling a service whose source is not mirrored just breaks `apt update`; `pro` took its URLs from the enclave contracts server and pointed the new sources at `svc-repo-01`, not the internet. ⚠️ **Enabling immediately surfaced three security updates that had been silently unavailable** — `opensc`, `opensc-pkcs11`, `prometheus-node-exporter` (all `+esm`), now installed on host-1/2/3 with `--force-confold` so the exporter ARGS survived; `fixups --apply` re-run after (patching reverts controls), 15/15 scrape targets up, facts from 8/8 | ✅ done |
| **3.7** | **Root CA key custody** | A policy question, not a mechanism | ⬜ open · `ssp-inputs.md` §2.5 |
| **3.8** | **Retire or justify the second USB disk on host-4** | The old WD easystore (`sdb`, 4.5 T, LUKS) is still plugged in and unmounted. Either a free second copy or a USB device on the hypervisor for no reason | ⬜ open |
| ~~**3.12**~~ | ✅ **CLOSED 2026-09-21.** `enable-tls.sh` re-run on svc-mgmt-01, svc-repo-01 and svc-obs-01 with the already-installed chain (no re-issue); all three now read `server_name svc-X svc-X.enclave.internal _;`. Contracts server still 200, Grafana 302, mirror 200 | ✅ done |
| **3.14** | **V-270755 re-scan on the four service VMs — bookmarked** | The 2026-09-19 service-VM rescan read V-270755 (wireless) **Open on all four — a bug in the answer code, not a finding**: it counted the idle `cfg80211` framework module as a radio ("Physical radio(s): ." — empty list). `answerfile.sh` fixed (framework modules `cfg80211`/`mac80211`/`bluetooth` no longer count; a real driver such as `btusb` still opens it), tested in pwsh on three cases. **Remaining:** `answerfile.sh generate` + `verify`, `stig-tools.sh answers` to the 4 VMs, re-scan. **Deferred 2026-09-19 — the scan ties up a terminal ~15 min per VM; ride it along with the next full scan round** (svc-obs-01 also owes the logrotate fixups for V-270756 before that round) | ⏸️ bookmarked |

## 4. Documents — 6 of 10 baseline artifacts not started

| Artifact | Notes | Status |
|---|---|---|
| `ssp.md` | The centre of the package. Blocked on the control baseline — **and the AO is Brian for now, so he can unblock it** | 🔴 blocked on a baseline decision |
| `sar.md` | **Generate from the CKLs, do not write** | ⬜ |
| `sap.md` | Procedure exists (runbook §6.0, §10.1); needs assessor independence and a schedule | ⬜ |
| `ir-plan.md` | Write for the inherited case by default | ⬜ |
| `rar.md` | Raw material is good — recovery-path analysis, the TPM trade, replica-3 | ⬜ |
| `boundary.md` | Text exists in runbook §1.2/§3.1. The diagram is not mine to draw | ⬜ |

✅ Written 2026-09-17: `poam.md`, `cm-plan.md`, `contingency-plan.md`, `iscm-strategy.md`.

## 5. Questions

**The AO's** (Brian, for now) — `docs/open-questions.md`:

| | Question | Status |
|---|---|---|
| **Q25** | Five Not Reviewed controls needing a signature. The audit-offload half (V-270658/V-270817) converts straight into engineering the moment it is answered | ⏸️ deferred 2026-09-18 |
| **Q22** | How much usable persistent storage the workload needs. Drivers now named — **map storage dominates, call volume is light** — and a sizing calculator is coming. Still blocks the Ceph OSD size and the BOM | ⏸️ deferred 2026-09-18 |
| **Q13 / Q15** | FIPS posture wording for the SSP. Largely answered by the Q17 measurement; **needs writing up, not deciding** | ⬜ open |
| **Q6** | Offline patch bundle size and cadence | ⬜ open |

**External, nobody here can answer:**

| | Question | Who |
|---|---|---|
| **Q14** | When 24.04 FIPS 140-3 validation completes | NIST / Canonical |
| **Q10** | The 1.36 LTS support window end date | Canonical |
| **Q4** | Unlimited VMs confirmed in writing | Canonical reseller |

## 6. 🔴 MAAS does not work under FIPS

Found 2026-09-17, runbook §9b. **Nothing in the current build path depends on MAAS** — `03-compose-vm.sh` composes from `virsh` directly — so this is not blocking. Three options: pursue Canonical, test a newer version, or remove it from the boundary. **Plan as though removing it is the answer.**

## 6a. ⬜ STIG/SRG assessment — opened 2026-09-18

| | Item | Notes | Status |
|---|---|---|---|
| **6a.1** | 🔴 **cyber.mil download list — CAC required, next trip** | See the table below — it is short and two of the items now block real work | ⬜ **needs a CAC trip** |

### 🔴 6a.1 — cyber.mil download list (CAC-only) — verified 2026-09-18

Versions and the nginx/Harbor question were **looked up rather than guessed**; sources at the
bottom.

| # | File | Why | Blocks |
|---|---|---|---|
| ~~1~~ | ✅ **OBTAINED 2026-09-18 — `U_Kubernetes_V2R6_STIG.zip` (Manual, xccdf dated 12 Feb 2026).** ⚠️ **Correction:** this list said *"V2R5 confirmed current"* — wrong; **V2R6 is current.** Measured 2026-09-19: **92 rules (18 CAT I)** vs the SCAP benchmark's 61, which is still **V2R5**. Every SCAP rule is in the manual; **31 are manual-only and need hand review, 12 of them CAT I** — incl. kubelet anonymous auth off (V-242391), explicit kubelet authorisation (V-242392), readOnlyPort off (V-242387), Pod Security Admission (V-254800/254801), **Secrets encrypted at rest (V-274882)**, Secrets not in env vars (V-242415). Applicable-set 800-53 coverage **115 → 116**; **156 with ASD** | K8s build (2.5) — the manual-only CAT Is are design decisions to make **before** the cluster exists, not findings to fix after |
| ~~2~~ | ✅ **OBTAINED 2026-09-18 — `U_BIND_9-x_V3R3_STIG.zip`, 73 rules / 43 CCIs / 3 CAT I.** Adds **26** 800-53 controls to coverage, taking the applicable set from 109 to **115** | 🆕 **New requirement created 2026-09-18** by standing up `bind9` on `svc-mgmt-01`. **Evaluate-STIG ships NOTHING for BIND** — its only DNS content is `U_MS_Windows_Server_DNS_STIG`, which is Microsoft DNS. **A BIND 9.x STIG does exist; the V3 series is current (V3R3, 01 Jul 2026)** — take the latest, the filename pattern is `U_BIND_9-x_V<n>R<n>_STIG.zip` | A DNS server in the boundary with no benchmark is an unassessed component |
| ~~4~~ | ✅ **OBTAINED 2026-09-18 — CAL dated 01 Sep 2026, EXPIRES 15 Nov 2026.** Four files: `CAL_by_Port`/`CAL_by_Service` PDFs, `CAL_Excel_format` xlsx (the one the generator reads — sheets: CAL by Port/Service, Exceptions, **CLSA**, Temporary) and `CAL_Record_of_Changes`. ⚠️ The workbook says **do not distribute the Excel format outside PPSM CCB/TAG membership** — it stays in gitignored `stigs/`, and generated output quotes only rows matching our ports | ⏰ **re-download before 15 Nov 2026** | 6a.22 |
| **3** | ✅ **OBTAINED — `U_ASD_V6R4_STIG.zip`. 🔴 IN SCOPE, not held in reserve** | **286 rules / 225 CCIs / 34 CAT I** — the largest document in the set. **AO 2026-09-18, from prior experience: *"I know 100% I will have to do that one — a lot end up being not applicable but I still have to fill it out."*** So the deliverable is a **completed checklist with a defensible rationale on every row**, not an exclusion argument | ⬜ **286 rows to answer** |

#### How the 286 ASD rows get answered without writing 286 justifications

✅ **139 of 286 rules (49%) carry their own "not applicable" clause in DISA's CheckText.**
For those the rationale is **DISA's own words plus a statement that the condition holds** —
quotable, consistent, and generatable. That is the cheap half, and doing it by hand is how
286 rows end up with 286 slightly different wordings for an assessor to pick at.

The remaining 147 need judgement, but they **group**. Measured theme counts (rules can match
more than one — this is for grouping, not arithmetic): database/SQL **28**, no-locally-developed-
code/COTS **17**, source code and SDLC **17**, DoD PKI/certificates **14**, web service/API/SOAP
**8**, mobile code **2**.

**The plan: answer ~12 themes once each, then apply. Not 286 individual write-ups.**

⚠️ **Two themes will NOT be N/A and should be expected to bite:**
- **database/SQL (28)** — PostgreSQL 16 is real, and `pg-01..03` are being built. These
  overlap the Postgres 16 STIG and need reconciling, not waiving.
- **DoD PKI / certificates (14)** — same private-CA problem as everywhere else in this
  enclave. AO acceptance, not a technical answer.

❌ **A DNS SRG is NOT needed.** The only DISA "DNS STIG" is **V4R1.20 from 2017** and is
superseded by the BIND 9.x series. **A product STIG exists for our DNS server**, so the SRG
fallback does not apply — this is the opposite of the nginx situation.

✅ **CONFIRMED: no DISA STIG exists for nginx, Harbor, Grafana or Prometheus.** That is now a
checked fact rather than an assumption, which materially strengthens the applicability
determination — "we searched and there is none" is a far better sentence for an assessor than
"we assumed there wasn't". It validates assigning nginx to the **Web Server SRG** and
Harbor to the **Container Platform SRG** under each SRG's own "products for which STIGs do not
exist" clause.

✅ **Not needed:** Ubuntu 24.04 V1R6 ships with Evaluate-STIG. Ubuntu 22.04 V2R9 was downloaded
but is **not applicable** — this enclave runs 24.04.

**Sources:** [DoD STIG index](https://cyber.trackr.live/stig) ·
[BIND 9.x STIG](https://www.stigviewer.com/stigs/bind_9x) ·
[Kubernetes STIG V2R5 checklist](https://ncp.nist.gov/checklist/996) ·
[Cyber Exchange downloads](https://www.cyber.mil/stigs/downloads)
| **6a.2** | **The control baseline and overlay set** | The gap count is meaningless without it — the tool's 1,014 denominator is the whole 800-53 catalogue, not an IL5 baseline. **AO input; Brian is the AO for now.** Also what `ssp.md` is blocked on | 🔴 **blocks the gap number AND `ssp.md`** |
| **6a.3** | **Assess Container Platform SRG against `svc-harbor-01` NOW** | Not a future item. By the SRG's own definition (engine + registry + key-value store) **Harbor-under-Docker is already a container platform**, missing only the keystore. No Harbor STIG and no registry SRG exist, so this SRG **is** the instrument. ~140 rules after tailoring | ⬜ open |
| **6a.4** | **V-233201 — local cache of PKI revocation data** | ⚠️ **The air gap makes this MANDATORY and the hardest PKI rule in the set, not N/A.** With no OCSP reachability, CRLs must be couriered in on a defined cadence or every certificate validates against stale revocation state | ⬜ open · needs a cadence decision |
| **6a.5** | **V-233233 — registry images patched within 30 days** | Demands a **≤30-day sneakernet cadence, permanently**. Operationally the hardest requirement in the document for this enclave. Not waivable on air-gap grounds | ⬜ open · pairs with the weekly Trivy decision (§3.2) |
| **6a.6** | **V-278968 — is `docker.io` from Ubuntu universe "vendor supported"?** | CAT I. Whether Ubuntu Pro `esm-apps` coverage of `universe` satisfies "a version supported by the vendor" for a container runtime is a question for **Canonical and the AO**. Do not assert either way without a Canonical source | ⬜ open |

| **6a.7** | **pg-01..03 build must start from §9a.2a, not §9a.3** | Four traps found 2026-09-18 *before* the build: Patroni's default `md5` replication user is an **instant CAT I on all three nodes** (V-261892); V-261967 contradicts 25 other rules over `log_destination`; ~60 fixtexts tell you to edit files Patroni regenerates; and 13 filesystem checks point at **RHEL paths that do not exist on Ubuntu** | ⬜ **read before building** |
| ~~6a.8~~ | ✅ **RESOLVED 2026-09-18 by inspection.** `patroni` 3.2.2-2 and `python3-etcd` 0.4.5-4 downloaded from the mirror and read: **zero `hashlib` imports and zero `.md5()` calls outside test code.** The MD5-under-FIPS failure mode is not present. Also corrected a wrong CAT I claim — Patroni generates **`scram-sha-256`** for PG≥10, not `md5`. ⚠️ A real failover under FIPS is still owed as proof; static analysis lowered the risk, it did not test it | ✅ risk resolved · failover test still owed |
| **6a.9** | **Decide pgcrypto vs LUKS for Postgres at-rest, in writing** | V-261901/930/931 (two CAT I) name pgcrypto. **pgcrypto is not inside any FIPS validation boundary** and its `crypt()`/`gen_salt()` fail at runtime under FIPS OpenSSL. LUKS at the VM disk layer is the defensible answer — but the substitution has to be *stated*, not implied | ⬜ open |
| **6a.10** | **Author the Postgres org-defined baseline** | 27 of 111 rules compare against a site baseline — approved extensions, superusers, object owners, port, pinned package version, per-role connection limits. **This baseline is the real deliverable** and is the tailorable artifact that survives to the next engagement. Missing baseline MUST yield Not Reviewed, never NotAFinding — 27 chances for a hollow pass | ⬜ open |
| **6a.11** | **Record the Crunchy-vs-PGDG tailoring statement** | This is the **Crunchy Data** Postgres 16 STIG. If pg-01..03 run Ubuntu/PGDG Postgres, applicability is a tailoring decision and V-283674's "vendor supported" reads differently. Defensible either way — but it must be written down, not assumed | ⬜ open |

| **6a.12** | **Web Server SRG — ASSESS, against 3 machines in 2 profiles** | System nginx runs on **`svc-repo-01`, `svc-obs-01`, `svc-mgmt-01`**. ⚠️ `HANDOFF.md:377` lists only `svc-repo-01` — **wrong, understates scope by two machines**. `svc-harbor-01` has **no system nginx** (measured); Harbor serves TLS from bundled containers — appliance-internal, scope it in writing. Two profiles: *static file server* (`svc-repo-01`) vs *reverse proxy* (other two) — the cookie/proxy/HTTP2 rules are N/A on the first | ⬜ open |
| ~~**6a.13**~~ | ✅ **CLOSED 2026-09-21 — all three, no deviation left.** SV-206414: `ssl_session_timeout` 1d → `${TLS_SESSION_TIMEOUT:-8h}` (the rule is "eight hours or less"). SV-206411: `autoindex off` with a default page for EVERY directory in one directive — **verified: `/`, `/tools/`, `/keys/`, `/debs/`, `/snaps/` and the ESM pool dirs all return the page with ZERO links**, while `InRelease`, `SHA256SUMS` and the tarballs still serve 200. The `/tools/` deviation opened this morning was closed the same day by 6a.25. SV-206412 **remains partial and is stated as such**: `server_tokens off` removes the version and OS (`nginx/1.24.0 (Ubuntu)` → `nginx`); dropping the product name needs the unmirrored `headers-more` module. **Two consumers broke and were fixed on the way:** `pro enable` fetches a directory URL (403 → default page), and `stig-tools fetch` walked the autoindex (→ tarball) | ✅ done · 206412 partial by design |
| **6a.14** | **Two nginx items that are hygiene, NOT vulnerabilities** | ⚠️ Recorded so nobody re-raises them as urgent. `nginx.conf` ships `ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3` at `http{}` — but **TLS 1.0/1.1 do not negotiate**: every 443 block carries a `TLSv1.2 TLSv1.3` override AND the FIPS provider refuses them at the library level (both measured). Likewise **no `ssl_ciphers` is set**, yet the negotiated suite is `TLS_AES_256_GCM_SHA384` over TLS 1.3. Fix both as defence in depth — a future server block without the override would inherit the weak default — but they are not live exposure | ⬜ low priority |
| **6a.15** | **🔴 V-264363 has NO technical remedy** | HTTP/2 is required end to end, but **nginx cannot speak HTTP/2 to an upstream** — `proxy_pass` is HTTP/1.1 at best, `grpc_pass` is the only h2 path and does not apply. So the proxy machines **are** doing the downgrade the rule prohibits. The rule's own text opens the only door: CAT III if the rewritten request is validated against the HTTP/1.x spec. **Needs probe evidence, not assertion** — nginx 1.24's rejection of CL+TE and malformed request lines is unverified | ⬜ open · needs a documented mitigation |
| **6a.16** | **⚠️ V-206430 becomes unfixable the day mTLS is enabled** | The rule demands client certs from **DoD PKI**. The enclave CA is private. As written it **admits no compensating control** — it needs AO risk acceptance, not a config change. N/A today only because nginx authenticates nobody. **Flag to the AO before anyone turns on client-cert auth, not after** | ⬜ open · AO decision |
| **6a.17** | **Database SRG — EXCLUDE, carry a 34-rule delta** | 108 of its 142 requirements have a Postgres 16 STIG child — pure double-count. **34 have no child**, incl. 2 CAT I (V-206555 password complexity, V-206561 obscure auth feedback) and 21 Rev-5 additions (V-263602–263622). Assess the product STIG **plus the named delta**, which is the fallback rule applied exactly as written. Ready-to-use exclusion rationale drafted | ⬜ ready to write up |
| **6a.18** | **Application Server SRG — EXCLUDE outright** | Nothing in the enclave meets the SRG's definition (a runtime environment hosting organisationally developed code). Harbor → Container Platform SRG; Grafana → Web Server SRG; nginx → Web Server SRG. ⚠️ **But see 6a.19 — do not let the exclusion drop an orphan control** | ⬜ ready to write up |
| **6a.19** | **🔴 CCI-000174 / AU-12(1) is covered by NOTHING else** | Time-correlated, system-wide audit aggregation appears **only** in the Application Server SRG (V-204716) — in no other guide we hold. It lands squarely on the open audit-offload thread. **Assess AU-12(1) as an RMF control at system level** in the SSP rather than importing 137 rules to catch one. Same treatment for CCI-002363 (AC-12(1)) and CCI-002169 (AC-3(7)) | ⬜ **do not lose this in the exclusion** |
| ~~6a.20~~ | ✅ **DECIDED BY THE AO 2026-09-18 — community PostgreSQL, tailored Crunchy STIG.** Condition verified: `postgresql-16` is in Ubuntu **main** (no licensing, Canonical-supported to 2036 under ESM) and depends on `libssl3t64`, so it inherits the FIPS provider structurally. pgaudit and patroni available from the mirror under `esm-apps`. Assess 111 tailored rules + the 34-rule Database SRG delta. Tailoring written up front in `ssp-inputs.md` §4.1g | ✅ done |
| **6a.21** | **ASD target is a COTS application — re-weigh the answers before filling them** | ⏸️ **Parked 2026-09-18 by Brian to finish later.** Read from DISA's own `U_ASD_V6R4_Overview.pdf` §1.1, not assumed: **COTS does NOT exempt** — *"This guide is to be used for in-house application development and to assist in the evaluation of the security of COTS/GOTS/OSS products and any other third-party applications."* What it changes is **who answers**: *"Answers to some questions will need to be gathered through discussions with vendor representatives or from public information about the company."* Measured in the XCCDF: **15 rules name COTS/vendor/third-party; 37 are about the development process** (code review, SDLC, scanning) — those become a **vendor questionnaire**, not N/A. Only **2** rules give COTS a carve-out: APSC-DV-003236 (N/A if the dev team is not accessible) and 001995 (N/A if the vendor will not provide code-review results). ⚠️ **Two CAT I rules get harder, not easier:** APSC-DV-003240/003250 need proof every component is vendor-supported. ⚠️ **APSC-DV-003080 is a likely finding** — backup copy of the software stored offsite or fire-rated; every copy is in one room (§1.3). ⚠️ **The example at the top of `asd-answers.md` (*"no locally developed application; vendor products only"*) is the wrong framing** — the ASD target is the COTS mission application, not "nothing"; platform components stay under their product STIGs/SRGs per Overview §1.1. **To do:** name the application and vendor, add an *answered-by* (system owner / vendor) split to `asd-triage.py`, fix the header example, draft the vendor questionnaire. 🆕 **Prior JITC checklist found 2026-09-18** — `docs/compliance/stigs/CAD MAP App Sec Dev STIG.cklb` (gitignored; **OMNI CAD/MAP**, ASD **V6R3**; same 286 rule IDs as V6R4, 6 check texts changed: 000110/120/130, 002330, 002900, 003120). JITC result: NaF 73 · NA 109 · Open 33 · NR 71. **Reusable as evidence: only 47 rows** have text about the application itself. **19 NaF/NA rationales rest on JITC's platform** (RKE2/Rancher, Active Directory, Ubuntu 20.04 CAC, FIPS 140-2 cert numbers, Bitnami Postgres, RAE-Syslog) and must be rewritten for ours — ⚠️ several may **flip to Open** here, because this enclave has no AD and no CAC yet. 211 rows are status-only with no rationale; 9 are drafts ("?????"). **The 33 Opens are mostly application behaviour** (8 CAT I: password length/storage, session destroy, command/SQL/input injection, overflow, default passwords) — controlling the host does not close those; they go to the vendor. Of the Opens, the ones the host/organisation *can* own: 003320 DoS, 000950 destination-IP logging, 001430 unsigned patches, 001480 program execution, 003130/003160 test procedures, 003260/003340 notifications. ⚠️ Confirm the application **version** matches what JITC assessed before reusing any application-side row. 🆕 **ON THE DoDIN APL (read 2026-09-19 from `DoDIN APL Report_18-Sep-2026.pdf`): MicroAutomation, Inc. — Omni911 4.1 — TN 2404602 — effective 15-Oct-2025, expires 15-Oct-2028 — sponsor OSD — Customer Premises Equipment.** That changes the ASD posture: the vendor has been through DISA APL testing, the JITC V6R3 checklist (May 2025) is Brian's own work — he did the JITC assessment **together with the vendor** (he is not MicroAutomation), five months before the listing, and APSC-DV-003240/003250 "vendor supported" has a DoD record behind it. **Deploy 4.1 or the listing does not cover us.** The platform products (Ubuntu, Canonical K8s, Postgres, Harbor) are not on the APL — it lists 169 network/UC/security devices, not general-purpose software, so absence there is not a finding | ⏸️ parked · Brian |
| **6a.22** | **PPSM — ports, protocols and services: scan, then generate the CLSA** | Requested 2026-09-18 as part of the security-posture paperwork. **The artifact has a name:** V-270719 asks for the *"PPSM Components Local Services Assessment (CLSA)"* and fails anything *"prohibited by the PPSM CAL"*. DoDI 8551.01 (31 May 2023) requires PPS used *"internal or external to the enclave"* to be declared; PPS confined to the enclave get a CLSA instead of a DISN vulnerability assessment. **Method, all from what exists:** (1) `ss -tulpnH` on every machine is the authoritative listener list, TCP **and UDP**, including bind address and owning process; (2) `nmap` from `svc-obs-01` proves what is actually reachable. **nmap 7.94 is already in the mirror** (universe, so `esm-apps`); (3) join both with the `stig-tailor.sh` ufw table, where **every allowed port already carries a justification** (tab-separated: machine · port/proto · action · source · reason); (4) join with the CAL to mark each row *on the CAL* / *not on the CAL, needs approval*. **Output is generated, never typed**, per baseline README rule 2: `docs/compliance/baseline/ppsm-clsa.md`. ⚠️ **The CAL is a CAC download from cyber.mil**. Listed as 6a.1 #4; its file format is unknown until you have it. ⚠️ Listening-but-unjustified and justified-but-not-listening are both findings to report, not tidy away. Also feeds ASD APSC-DV-001510/002990 and closes POA&M ENG-53 🆕 **CAL read 2026-09-19. The answer for an enclave-internal system:** at boundaries 09–16 (enclave-side) **every CAL and CLSA entry reads `AO`** — approval is the AO's, not DISA's; the YELLOW/GREEN/RED colours apply only at DISN boundaries 07/08, which the air gap never crosses. **Match by SERVICE, not port** — wide-range entries (e.g. ORACLE-OCR) cover thousands of ports and give false hits. By name: SSH, DNS, NTP, HTTPS, `PGDG-POSTGRESQL-DB` 5432 and `KUBERNETES-KUBELET-API` 10250 are **on the CAL**; Prometheus, node-exporter, Alertmanager, Grafana :3000, etcd, Patroni, Harbor, libvirt, Ceph are **NOT** — they appear only as *other organisations' CLSA entries* (e.g. DLA-GRAFANA 3000/tcp), which is the precedent: **we write our own CLSA rows and the AO approves them.** ⚠️ **DESIGN CONSTRAINT (Brian, 2026-09-19: *"how will we run onsite"*):** it must run **entirely inside the gap** — scan, collect and generate from an in-gap machine (`svc-obs-01` is the natural home: collector and nmap vantage), CAL carried in with the transfer bundle. **`stage-01` may be a lab convenience, never a dependency** — it is outside the boundary and unplugged at cutover. A first design that rode on `stig-tools.sh collect` was stopped for exactly this reason 🔄 **BUILT 2026-09-19, steps 1–3 of 5:** (1) `monitoring.sh facts` publishes `enclave_listen_socket` and `enclave_ufw_active`/`enclave_ufw_rule` from all 8 machines, as root, every 15 min — no SSH key, no stage-01; (2) `scripts/enclave/ppsm-services.tsv` maps each listener to its exact CAL service name, built from the measured listeners; (3) `scripts/enclave/ppsm.py` joins facts + design table + services + CAL into the CLSA. **First run: 72 listeners (33 external), 36 findings — 14 UNFILTERED (host-4, svc-harbor-01, svc-mgmt-01 have ufw off), 9 NO DESIGN RECORD, 13 AO APPROVAL (9100/9177 exporters and 8484 are not on the CAL).** It also caught two stale design reasons in `stig-tailor.sh`, fixed. (4) `ppsm.py --scan` built and tested with a synthetic scan: the scan result overrides the bind-address assumption (new finding BOUND BUT NOT REACHABLE), and anything answering that no machine reported is OPEN, NOT IN FACTS (UDP 623 IPMI is always probed). ⚠️ **nmap becomes a package in the boundary on svc-obs-01** — record in the CM plan as the assessment tool. **(4) RUN 2026-09-19 03:43–03:46 UTC from svc-obs-01: 8 hosts, every TCP port + 17 UDP, 183 s.** ✅ **8484 on svc-mgmt-01 is `filtered`** — the runbook's IPAddressDeny claim is now measured, not assumed. ✅ **Nothing answered that no machine reported** — no stray service, **no BMC on 623/udp**. ⚠️ Two limits of the scan, now labelled in the report rather than misread: (a) **ufw LIMIT is tripped by the scan itself** — host-1/2/3 9100/9177 read `filtered` while Prometheus scraped them 49/49 through the scan window; svc-repo-01 9100 (an `allow` rule, same source) read `open`, which isolates the cause; (b) **svc-obs-01 scanning itself goes over loopback**, so its row is not an outside view (`--scanner`). 🔄 **(5) AO paperwork DRAFTED 2026-09-19 — generated, not typed.** The 13 AO APPROVAL rows are **3 services**; `ppsm.py` now writes an *AO decision* section with one proposed local CLSA entry each, in the CAL workbook's own CLSA-sheet columns (Network · Service name · TCP/UDP · Low/High · ORG · boundaries 11/12 = AO), the measured machines, a risk paragraph, precedent and a signature line: `«PPSM_ORG»-PROMETHEUS-NODE-EXPORTER` 9100/tcp ×8 (precedent: DMDC-PROMETHEUS, DHA-MULESOFT-PCE-PROMETHEUS, MDA-…-NODE-EXPORTER) · `«PPSM_ORG»-PROMETHEUS-LIBVIRT-EXPORTER` 9177/tcp ×4 (no precedent) · `«PPSM_ORG»-CANONICAL-UBUNTU-PRO-CONTRACTS` 8484/tcp (host-local; scan-proven filtered). Name and risk text live in two new `ppsm-services.tsv` columns; `PPSM_ORG` added to `facility-profile.env`. Precedent matches port **and** name — port alone listed rendering services at 9100. ⚠️ **Disclosed in the risk text: both exporters are plain HTTP with no auth** (no `--web.config.file`) and node-exporter now publishes each machine's port inventory and ufw rules — TLS from the enclave CA is the available mitigation, not built. ⚠️ **Evaluate-STIG deleted-in-waiting:** its rotation moved `SVC-OBS-01/PPSM/` (CLSA, nmap XML **and the CAL copy**) into `Previous/` at 03:50, and `PreviousToKeep=1` means the next scan deletes it. PPSM output now goes to `/srv/stig-evidence/PPSM/`; `ppsm.py` refuses an `--out` inside an Evaluate-STIG host tree. ✅ **Fresh scan 2026-09-19 19:10 UTC** (`/srv/stig-evidence/PPSM/ppsm-clsa-20260919-1910.md` on svc-obs-01; CAL and the 03:4x evidence rescued from `Previous/`): 21 findings, **every UNFILTERED is host-4** (22, 123/udp, 9100, 9177), nothing answers that no machine reported, 8484 filtered. **Remaining:** fill `«PPSM_ORG»`/`«AO_NAME»`, AO signs. 8484 still reads DEAD LISTENER + NO DESIGN RECORD — true but by design; decide whether to record it in the ufw table as an explicit deny | 🔄 step 5: drafted and re-scanned, awaiting AO |
| **6a.23** | **CAC/PIV access — plan drawn up 2026-09-18** | **In scope per Brian 2026-09-18** (*"at DISA we had to make CAC access work"*). That answers open-questions CAC item (a). Closes the five-control smart-card family (V-270663/735/736/722/745, incl. the **only HIGH left**), and turns 6a.16 from risk acceptance into a fix. Everything is in the mirror: `sssd` 2.9.4, `libpam-sss`, `sssd-ldap`, `opensc`/`opensc-pkcs11`, `pcscd`, `libccid`, `slapd` 2.6.10, `389-ds-base`. USB lockdown blocks **storage only** (`usb-storage`/`uas`), so CCID readers work. **Open decisions: D1** identity source: local users + SSSD certmap (Canonical-documented, but V-270736's check literally greps `ldap_user_certificate`, so it becomes a documented deviation) **vs** an in-enclave LDAP directory (closes the HIGH as written, adds a component to assess). **D2** trust anchors are a *facility parameter*: DoD roots for CAC, Federal PKI for PIV. **D3** CRL carry-in cadence, shared with 6a.4. **D4** an in-gap admin workstation with a reader. None exists today (POA&M ENG-54). ⚠️ **`docs/compliance/stigs/UbuntuRootCertInstaller.docx` checked 2026-09-18: DO NOT USE.** Measured against the real v5.14 bundle: its `*.pem` glob selects only `DoD_PKE_CA_chain.pem` (Root CA 6 + DOD EMAIL CA-73, the code-signing chain), skipping all 49 certs in the `.p7b` files, so Root CA 3/4/5 and every issuing CA are missing; `openssl rehash` skips that file (*"does not contain exactly one certificate"*); it downloads inside the gap; it skips the bundle signature check the README requires; it still prints Success and would **hollow-pass V-270745**. **Brian is modifying it himself (2026-09-19)**. Do not build a competing `ca.sh` subcommand; review his version against these points when he brings it back. Phases and detail: see the plan when written into runbook §10.1d | ⬜ **4 decisions, then build** |

| **6a.24** | **STIG evidence collection depends on `stage-01` — it has no onsite home** | Found 2026-09-19 while designing 6a.22. `stig-tools.sh collect` pulls every machine's results to `stage-01` using the `build01` key; it only *warns* when run elsewhere. Onsite there is no `stage-01`, so scan evidence would stay scattered on the machines that produced it — the 2026-09-14 problem again. Needs an in-gap evidence home (likely `svc-obs-01`), a key that exists inside the gap, and the same design shared with 6a.22 | ⬜ open · decide with 6a.22 |
| ~~**6a.25**~~ | ✅ **CLOSED 2026-09-21, same day it was opened.** `stig-tools.sh publish` now packs Evaluate-STIG into **one tarball — 114 MB tree → 21 MB**, transferred in seconds — writes its sha256 into the existing `SHA256SUMS` beside PowerShell's, and removes the published tree. `fetch` downloads two files, verifies the checksum, extracts, and **refuses to extract on a mismatch** — something a 390-file walk could never do. Falls back to the old walk with a loud warning if a mirror predates this. **Measured on host-3: `Evaluate-STIG.tar.gz checksum verified`, 390 files installed, with `autoindex off`.** ⚠️ Also fixed: the publish check probed a file inside the tree it had just replaced, so a healthy publish reported 404 | ✅ done |

⚠️ **Do not over-claim air-gap N/A.** Of 188 Container Platform SRG rules, only **8** are genuinely not applicable. The air gap changes *how* the rest are satisfied, not *whether* they apply — and 6a.4 is the clearest case of a rule that gets **harder**, not waived.

## 6b. ✅ MAAS removed 2026-09-18 — what it cost and what it left

**Open listeners on `svc-mgmt-01`: 76 → 21.** Remaining ports are `22 25 53 80 123 323 443 5432
8484 9100` — ssh, postfix (loopback), DNS, nginx redirect, time, TLS, postgres, the Pro contract
server, node-exporter. 13 packages and 10 services gone.

✅ **Ubuntu Pro survived**, which was the gate: `pro refresh` succeeded with MAAS fully down, and
`esm-apps`, `fips-updates` and `usg` stayed enabled. The contract server is independent of MAAS.

| | Item | Status |
|---|---|---|
| **6b.1** | ⚠️ **`autoremove` took `bind9` with MAAS** — its only reverse-deps were MAAS packages. `named.service` reported `Loaded: not-found`, which reads like a broken config and is a missing package. Zone files in `/etc/bind/enclave/` survived; reinstall + `zone-install` is the recovery. **`apt-mark manual bind9` before the purge on a rebuild** | ✅ recorded in runbook §9a.4 |
| **6b.1a** | ✅ **Enclave DNS is up on `svc-mgmt-01`, verified from host-4.** Forward, reverse and the `*.apps` wildcard all answer; out-of-zone names return **NXDOMAIN in 147 ms**. Zones render from the same `MAP` in `apply-addresses.sh` that writes `/etc/hosts` — one table, two renderers | ✅ done |
| **6b.1b** | ⚠️ **NOTHING points at it yet, and that is deliberate.** No `DNS=` on any machine; resolution is still `/etc/hosts` only. Pointing resolvers at `10.2.20.161` is a separate reversible step and is what makes `*.apps` and CoreDNS forwarding actually work | ⬜ **next DNS step** |
| ~~6b.1c~~ | ✅ **ALREADY FIXED 2026-09-03 — the note was stale, not the file.** `airgapped-contracts.yaml` carries 4 `.internal` references and zero `.local` in both staged copies, verified without printing it (it is a credential). Decisive evidence: the config's `aptURL` entries are what `pro` writes into client sources, and host-4's sources read `svc-repo-01.enclave.internal`. Runbook §9a.4 corrected | ✅ done |
| **6b.1d** | ✅ **Resolvers pointed at the enclave DNS — all 8 machines.** `DNS=10.2.20.161`, hosts-file names still resolve, the `*.apps` wildcard now resolves **via DNS**, outside names return nothing. ⚠️ **`svc-repo-01` carries its own FQDN on the `127.0.1.1` line**, so its own name resolves to loopback there — the only machine of eight. Benign (TLS validates on the NAME, not the address; apt works) and **not auto-fixed**: that line is outside the managed block. `apply-addresses.sh apply` now warns when it sees it | ✅ done · one cosmetic outlier |
| **6b.2** | **`postgresql@16-main` on `svc-mgmt-01`** — MAAS's database | 🔄 **PROVEN UNUSED 2026-09-21:** only `postgres`/`template0`/`template1` remain (MAAS's database is gone), the only connection was the query itself, and nothing depends on it but `postgresql-client-16`. **Stopped and disabled 2026-09-21** rather than purged — a stopped service proves nothing needs it and is reversible; a purge is not. **Purge after 2026-09-23** if nothing has complained, then drop the `5432 postgres PGDG-POSTGRESQL-DB` row from `ppsm-services.tsv` and regenerate the CLSA — it is one fewer service for the AO to approve | 🔄 stopped · purge + PPSM row after 2026-09-23 |
| ~~**6b.3**~~ | ✅ **CLOSED 2026-09-21.** `fixups --apply` item 0b disabled `sssd` (no non-empty `domains=`) and `openipmi` (no IPMI device) on all four service VMs and cleared their failed state; the packages stay installed so the STIG rules that want them still pass | ✅ done |
| ~~**6b.4**~~ | ✅ **CLOSED 2026-09-21.** All four service VMs got the fixups: `sysstat` PURGED (the decided answer — node-exporter already publishes what it collected, and its daily 0644 file re-opened V-270756 every day), the 2c logrotate `create` lines written, apt log perms re-tightened, ssh client configs readable. **svc-obs-01 reports V-270756 clean.** Then `v1r6 --apply` on the other three fixed the journal declaration; **svc-repo-01 and svc-harbor-01 VERIFY PASSED after a reboot**, Harbor 8/8 healthy, the mirror serves (`apt-get update` from another VM hit every repo), and svc-mgmt-01 came back with DNS, the wildcard, reverse and the contracts server (HTTP 200) all working | ✅ done |
| ~~**6b.5**~~ | ✅ **CLOSED 2026-09-21** with 3.6 — `esm-infra` enabled everywhere. ⚠️ **My 2026-09-20 status table was WRONG about host-4**: it was read by `awk` column position out of `pro status`, which shifts, and reported `esm-infra=disabled` when `pro` said already-enabled. Read `pro status --format=json`, not columns | ✅ done |

## 7. Never mirrored, never built

- **Landscape** and the **Enterprise Store** — never in a transfer bundle. Blocked on a transfer trip before they are blocked on writing. ⚠️ *State not re-verified since it was recorded.*
- **`svc-log-01`** — the audit-offload collector. `svc-obs-01` can host it instead; the blocker is Q25's three answers, not the missing VM.
- **A Trivy CLI on the machines** — Trivy exists only inside Harbor's container, so nothing can scan the hosts' own filesystems.

## 8. ❓ Unresolved: is the bake-off artifact still the deliverable?

`CLAUDE.md` names the **Canonical Kubernetes vs Spectro Cloud VerteX comparison artifact** as this project's deliverable. `artifact/artifact-source.html` has not been touched since **31 August 2026**. Everything since has gone into building the enclave.

**That may be exactly right — but it should be a decision, not a drift.** If it is still owed, its time-sensitive vendor claims need re-verification before it ships (`CLAUDE.md`, "Re-verify before anything ships"). If the build has replaced it, write that down so the next person does not find a three-week-stale document and assume it is current.

## 9. Recently closed — append only, newest first

- ✅ **3.18 CLOSED 2026-09-22 — the enclave's isolation is now enforced by a device the enclave does not control.** Firewall policy **21 `DENY enclave to WAN`** — srcintf `lab`, dstintf `wan1`+`wan2`, srcaddr `enclave-10.2.20.0` (`10.2.20.0/24`), `set action deny`, `set logtraffic all` — sequenced **above** policy 19 `Lab to WAN` and verified there (`show firewall policy | grep edit` -> 21 then 19; `grep -f lab` returns those two and nothing else). **This was impossible on the retired travel router**, which had no source-based rules; the runbook's third isolation layer had read *"add it if the WR3602BE supports source-based rules"* since it was written. ⚠️ **Stated honestly: this is not what blocks the enclave today** — there is no `lab -> wan1` permit policy, so enclave-sourced traffic already hit the implicit deny, and policy route 4 matches only `10.2.10.0/24` and `10.0.20.160/32`. The rule's value is that the deny is now **explicit, logged and auditable**, and that it **survives someone later adding a broad `lab -> wan` policy**, which would otherwise re-open the path silently. Layers 1 (no gateway) and 2 (`ip_forward=0`) are host-side and self-asserted; this one is not. Written up in `lab-network.md` §4 and `runbook.md` §3.1
**2026-09-22**
- ✅ **3.15 severity corrected by measurement — a lockout does NOT survive a reboot.** `faillock` has no `dir =` line, so records are in `/run/faillock` on **tmpfs**; a power cycle clears them, and with TPM unlock the host returns unattended. **Compliant:** V-270690 checks the policy keywords, not the storage location. **Residual:** someone must physically press the button (CUST-14, the BMC case), and **on host-4 a power cycle stops all four guests ungracefully** since the ordered shutdown needs a login. POA&M CUST-15 lowered **High → Moderate**; runbook §10e rewritten. ⚠️ **The first version of that procedure had me routing people through GRUB and rescue mode to a root prompt nobody can satisfy — root is locked. Verifying where faillock stores its state replaced a wrong, elaborate procedure with a correct, trivial one**
- ✅ **Full rescan, all 8 machines — V-270755 is GONE fleet-wide.** The corrected answer file plus the radio work closed it everywhere: hosts 171/5/9/9 (host-3 170/6, host-4 169/7), service VMs 171-172/4-5. **The four fleet-wide opens are now ALL AO decisions, not work:** V-270663, V-270735, V-270736 (smart-card/PKI family) and V-270817 (audit offload)
- ✅ **V-270816 closed on host-1 by MEASUREMENT, and it was a real finding.** auditd ships `max_log_file 8` x `num_logs 5` = **40 MB**; host-1 wrote **39.3 MB in 37.6 h (~23-25 MB/day)**, so the allocation held **1.6 days against a 7-day control** — on a volume with **19 GB free**. New `fixups` item 7 sizes the allocation from each machine's OWN measured rate (a week, doubled), refuses unless the volume has twice that free, and **SIGHUPs auditd** (Ubuntu refuses a manual restart and killing it drops records). host-1 after: **172 NF / 4 Open / 9 NR / 9 NA — the cleanest machine in the enclave.** Owed: the same apply on host-2/3/4 and svc-mgmt-01, then a rescan
- ⚠️ **Machine-specific leftovers from the round:** host-3 V-270756 (one `unattended-upgrades` file above 0640 — `fixups --apply` fixes it) · **host-4 V-270655: ufw INACTIVE** — that is 3.1, deferred by decision, now visible as an Open finding rather than a deferred row · host-4 V-270718 USB mass storage, expected while the backup volume window is open
- ⚠️ **V-270650 hit the scanner's 15-minute check timeout again** on host-1 (`Get-V270650`) — known, and the reason it sits in the answer file

**2026-09-21**
- ✅ **Radio consistency pass, all four hosts 2026-09-21.** `radio disable` re-run everywhere: both blacklist copies present, **verified inside the running initramfs on all four**, no blocked module loaded, no wireless interface. `wpasupplicant` purged from host-1/2/4 (dead weight — no radio on any of them) and `libxml2-utils` installed so the tailoring XML is validated. host-4 carries a MediaTek MT7921 rather than the Realtek the other three have; both are blocked
- ⚠️ **CORRECTED, same day: the radio blacklist WAS in the initramfs all along — the CHECK was lying.** `lsinitramfs | grep -c stig-radio` returns **2** on host-3 (both `etc/modprobe.d/` and `usr/lib/modprobe.d/` copies are packed), while `initramfs_has` reported it absent and `radio disable` warned *"the rebuild did not pick it up"*. **So the identical warning on all four hosts on 2026-09-17 was a false alarm**, and my first diagnosis today — that only `/usr/lib/modprobe.d` is packed — was wrong too. The real fault: listing a 77 MB multi-segment zstd initrd immediately after `update-initramfs` returns can come back short, and `2>/dev/null` hid the reason. Now it syncs, retries three times, accepts either path, and PRINTS the lsinitramfs error instead of swallowing it. The `/usr/lib` copy is kept as belt-and-braces. ⚠️ **A check that cries wolf on a correct machine is worse than no check** — this one was ignored for four days because it always said the same thing. Third swallowed-stderr bug of the week
- ⚠️ **(superseded by the line above)** The radio blacklist appeared never to reach the initramfs, and the check said so after every run. `radio disable` wrote `/etc/modprobe.d/99-stig-radio.conf` and then verified the initrd contained it — but `/usr/share/initramfs-tools/hooks/kmod` copies **only `/usr/lib/modprobe.d/*`** and never touches `/etc/modprobe.d`. So the warning *"the rebuild did not pick it up"* was permanent and unfixable, and it fired on all four hosts on 2026-09-17 and again on host-3 on 2026-09-21. **Fixed:** an identical copy is written to `/usr/lib/modprobe.d/99-stig-radio.conf` (the only path packed) and the check looks there; `/etc` keeps the authoritative copy, which is where DISA's CheckText points and which wins at runtime. **A check that cannot succeed teaches the operator to ignore warnings**
- 🔴 **`step_radio` was never dispatched — a step shown on the status board that `run` does not call.** `cmd_run` listed `tailor; grub; v1r6` and skipped `radio`, while `status` printed `[ ] radio` as if it were pending. The host-3 rebuild came up with its **RTL8821CE driver loaded, bluetooth loaded, `wlp2s0` present and no blacklist**, and V-270755 scored **Not Reviewed** ("a wireless interface is configured"). **host-1/2/4 have no radio only because someone disabled theirs BY HAND on 2026-09-17** — the automated path has never once done it. Fixed by dispatching it. **A displayed-but-undispatched step is worse than a missing one: the board says it is accounted for**
- ⚠️ **`sysstat` came back on the rebuild** — purged by hand on all eight machines (decision 2026-09-16) and encoded nowhere, so a fresh build reinstalls it and V-270756 re-opens daily. Now purged by `monitoring.sh exporter`, AFTER that script has installed and verified node-exporter's collectors — removing a collector before its replacement is proven working is how a machine ends up with neither
- ✅ **Yesterday's V-270755 answer-file fix behaved correctly on real hardware:** on host-3, which HAS a radio, the "no radio" answer did not apply and the rule stayed Not Reviewed rather than being falsely marked clean. Different answers from one file on different hardware is the proof it is right
- ⚠️ **Hardening died silently at step 8 on the first genuinely clean build.** `fixups --apply` ran `jtf="$(ls /etc/tmpfiles.d/*stig*.conf | head -1)"`; on a fresh host that file does not exist yet (`v1r6 --apply` writes it at step 12), so `ls` exits 2, `pipefail` carries it past `head`, and **`set -e` ended the script with no message** right after the journalctl line — `tailor` was never marked done. **Every host built before survived only because it already had the file from an earlier hand-run**, so no clean build had ever exercised the path. Fixed with `find ... || true`, and the missing file is now a NOTE naming step 12 rather than a failure that stops a sequence already going to fix it. A second instance of the same pattern (`find /var/log ... | sort`) fixed alongside. **This is the third swallowed-failure bug in two days and the second found by rebuilding rather than by reading** — the class is: an assignment from a failing pipeline under `set -euo pipefail`
- ⚠️ **The SV-206411 listings fix broke `pro enable fips-updates` — caught by the host-3 rebuild, not by my verification.** With `autoindex off` and no index, nginx answers a DIRECTORY URL with **403**, and the Pro client verifies repository access by fetching `.../fips-updates/ubuntu/pool/` — a directory. Result on a fresh host: *"403 Forbidden ... Could not enable FIPS Updates"*, hardening dead at step 3. **apt never noticed** because it requests explicit paths from the `Release` file, and apt is all I tested. **Fixed properly:** `enable-tls.sh` now gives EVERY directory a default page in one directive (`location ~ /$` → `try_files $uri/index.html @dirdefault` → `return 200 <page>`), which is closer to DISA's fix text than scattering `index.html` files and also covers directories a later mirror sync creates. **The lesson: a change to a shared service has to be verified against every CONSUMER, not just the one in front of you.** apt, `pro`, snap, Harbor and the STIG tool fetch from svc-repo-01 in different ways

**2026-09-20**
- ⚠️ **V-270757 remediation was gated on the SYMPTOM, not the DECLARATION** — `v1r6 --apply` rewrote `/etc/tmpfiles.d/zzz-systemd-stig.conf` only when a journal directory was already wrong. svc-mgmt-01's were right, so it kept the **stale two-line file from 2026-09-11** (the version that manufactured V-270762) and measured compliant with nothing holding the value: journald resets those modes on restart. Now the file's CONTENT is part of the condition — missing, or not DISA's four `z`/`Z` lines, and it is rewritten. ⚠️ Also: that file is 0600 on svc-mgmt-01 and 0644 elsewhere, so unprivileged reads of it fail — **twice tonight I read "permission denied" as "the file is absent"**; check the mode before concluding anything is missing
- ✅ **6b.4 fixups applied to all four service VMs, and 6b.3 closed with them** — `sysstat` PURGED on all four (the decided answer since 2026-09-16: node-exporter already publishes what it collects, and its daily 0644 file re-opened V-270756 every day); `sssd` and `openipmi` disabled with their failed state cleared, packages left installed; ssh client configs readable again; the 2c logrotate `create` lines written on all four. **svc-obs-01 reports V-270756 clean** after two 0644 `unattended-upgrades` files were corrected
- ⚠️ **Journal `%m` directory: three machines, three different states — the 09-18 fix never left the hosts.** `svc-obs-01` has the complete four-line `zzz-systemd-stig.conf`; **svc-repo-01 and svc-harbor-01 carry the STALE TWO-LINE version from 09-11** (parents only, no recursive `Z .../%m ~0640`), so their `%m` dir sits at 2750 and `fixups --apply` failed honestly; **svc-mgmt-01 has NO declaration at all** yet measures 0640 — `usg fix` chmod'd it once and nothing holds it, so a systemd restart silently re-opens V-270757. Fix: `v1r6 --apply` on those three. **Lesson repeated: a fix applied to the hosts is not applied to the VMs**
- ⚠️ **`fixups` plan did not list items 0b-0d, which `--apply` performs** — the operator approved a change set that was not the change set. The plan now prints them with measured state
- ⚠️ **The second copy needed `--delete`, found by measuring it** — 84 GB landed on host-1 (103 GB free of 196 GB). Without `--delete` the far end NEVER shrinks: `prune` drops old chains on host-4, nothing drops them there, and each new full adds ~84 GB until the volume fills and the copy silently stops. Now mirrors the source (`BACKUP_SECOND_DELETE=0` opts out). The trade — a mistake here propagates there — is why the copy is not the only control: the weekly deep verify and `restore-test` are what make it trustworthy
- ✅ **Second backup copy is live and in the nightly timer** — host-4 → host-1 over SSH 22 (already on the CAL, no new port), `rrsync -wo` restricted key, verified by re-reading the far end. First run 84 GB / 877 s. Appended to the nightly unit as a NON-FATAL last step (`ExecStart=-`): a far-end problem must not make a good backup look failed; `enclave_backup_second_*` is the signal instead
- ✅ **Guest restore proven, all four service guests** — see 2.1. CP-4 had no evidence of any kind before this; `poam.md` ENG-01 moves to partially satisfied
- ✅ **FIPS refuses ed25519 for SSH keys** — `ssh-keygen -t ed25519` on host-4: *"ED25519 keys are not allowed in FIPS mode"*. Approved types are RSA and ECDSA P-256/384. `vm-backup.sh second-copy --setup` now makes RSA 4096 (`BACKUP_SECOND_KEYTYPE` overrides). Worth remembering for the CAC/PIV work (6a.23) and any new key in the boundary

**2026-09-19**
- ✅ **2.2 Move `svc-repo-01` off host-4 and 2.3 Rebuild host-4 — SUPERSEDED by 2.6** (Brian: *"can this not be addressed when we rebuild the whole system from scratch"*). 2.3 existed to clear host-4's built-before-the-fix gaps — since fixed in place by fixups — and to prove the host build reproducible, which the from-scratch build proves better. 2.2 existed only to make 2.3 safe. Neither is worth an outage on the host carrying the mirror, DNS, Harbor, monitoring and the backup drive
- ✅ **Collector placement decided (recorded late; decided 2026-09-18): `svc-obs-01` stays on host-4.** Moving it to host-1 for AU-9(2) changes *which* four machines fail, not how many (host-4 + 3 guests vs host-1 + 3 guests at full build). On four hosts with everything virtualised no placement fully satisfies AU-9(2); the residual is a stated exception. Don't re-open without a fifth physical box
- ✅ **3.13 MAAS leftovers** — deviation and AIDE exclusion removed from `stig-tailor.sh`; svc-mgmt-01 orphans 61 → 0, BIND `managed-keys` permission fault fixed. **Rescan confirms: `sudo_require_authentication` passes, `check_ufw_active` passes on svc-mgmt-01 and svc-harbor-01.** Service VMs now: USG svc-mgmt-01 211/5, svc-repo-01 212/5, svc-harbor-01 211/5, svc-obs-01 210/6; V1R6 170/9/9/6 ×3, svc-obs-01 169/9/9/7. Open set is the smart-card family + audit offload, plus a false V-270755 (→ **3.14**) and svc-obs-01 V-270756 (logrotate fixups owed)
- ✅ **ufw enforcing on svc-harbor-01** — applied ~04:00 UTC, then **rebooted 04:04 to catch the Docker/ufw FORWARD ordering trap** (runbook 6.3e). After reboot: docker, harbor, ufw all active; Harbor API **healthy 8/8 components** from host-1 and from itself; **9100 from host-1 times out (DROP)**; Prometheus scrape up. ⚠️ **Partial control, as recorded in 6.3e:** ufw protects the host's own ports (22, 9100); Harbor's 80/443 are published by Docker and bypass ufw — the CLSA labels them BYPASSED. Filtering those means `DOCKER-USER` rules, separate work. **UNFILTERED in the CLSA: 13 → 4, all on host-4.** Expect `check_ufw_active` to pass on svc-mgmt-01 and svc-harbor-01 at their next USG audit
- ✅ **3.1 (svc-mgmt-01 half) — ufw enforcing on svc-mgmt-01**, applied 03:55 UTC. Table written from the MEASURED listeners (ppsm.py), not a port list: 22 limit; 53 tcp+udp, 123/udp, 80, 443 from 10.2.20.0/24; 9100 limit from svc-obs-01; 8484 absent (scan-proven filtered). **Verified from other machines:** DNS resolves via svc-mgmt-01 from host-1 and svc-harbor-01; HTTPS to the contracts proxy answers; Prometheus 15/15 up incl. svc-mgmt-01:9100; **and 9100 from host-1 now times out (5.0 s, DROP)** where it answered before — the rule enforces, not just the services survive. Clears 7 of the 13 UNFILTERED rows in the CLSA
- ✅ **3.11 Root timers executed encadmin-writable scripts** — a sudo bypass on 5 machines (facts ×5, vm-backup/verify on host-4, audit-volume ×3). Fixed by `install-runtime.sh`: units now run `/usr/local/lib/enclave`, `root:root`. **Verified on all 8:** every `enclave-*` unit points there, `find` for non-root-owned or group/other-writable files returns nothing, all copies from commit `9909102`. After every repo push, re-run the timer installer
- ✅ **3.10 host-1/2/3 published no compliance facts** — facts timer installed; Prometheus now has facts from **8 of 8** machines (was 5), every source `ok`
- ✅ **3.9 `ufw_rate_limit` on host-1/2/3** — 9100/9177 changed `allow` → `limit` (source still `svc-obs-01`). USG back to **213/3** on all three (audits 02:58–03:00 UTC); **15/15 Prometheus targets stayed up** across three checks 20 s apart
- ✅ **Rescan 2026-09-19** — V1R6 unchanged: host-1/2/3 171/9/9/5, host-4 169/9/9/7. ⚠️ The service VMs' latest USG audits are **3–8 days old** (09-11 to 09-16) per the facts — due a rescan

**2026-09-18**
- ✅ **Q28** — the third-party package list. The question's premise was wrong: "24–46 per machine" was the `universe` bucket, which **is** covered by `esm-apps`. The genuinely uncovered set is **five installs, four packages** — `contracts-airgapped`, `get-resource-tokens`, `pro-airgapped` (Canonical) and `grafana` (Grafana Labs). Docker is `docker.io` from universe, not `docker-ce`. Generated by `stig-tools.sh coverage`, never transcribed. `ssp-inputs.md` §4.1f
- ✅ **Q27** — Trivy DB refreshed weekly. Alert fires at 10 days, not 7, so a met policy does not alert. §4.1e → **left 3.2 owed**
- ✅ **Q26** — a monitored in-boundary alert satisfies V-270818/V-270819. Does not foreclose email if a path out is ever authorised. §4.1d
- ✅ **Monitoring on host-1/2/3** — they were invisible to Prometheus; the scrape list is hardcoded and building a machine does not add it. Now **15/15 targets up**, node and libvirt
- ✅ **V-270756 closed at the creation point on all four** — the cause was **logrotate**, not `sysstat`. Three stanzas recreated world-readable on purpose; twelve had no `create` line at all
- ✅ **V-270755** — every host had a live WiFi *and* Bluetooth radio. The checklist scored it Not Applicable on a machine with a radio in it
- ✅ **host-4 collected and caught up** — 166/10/11/7 → 169/9/9/7; all four hosts now match exactly on NA and NR, rule for rule
- ✅ **host-1/2/3 made VM-ready** — libvirt, `br0`, 300 GB pool, 200 GB data volume (runbook §6.5, a step that did not exist)
- ✅ **Seven tool bugs fixed**, each of which would have survived a rebuild: `update-initramfs` rebuilding the wrong kernel · `radio status` dying silently on every VM · `set_args` and its pipes · the textfile check reading `/proc/cmdline` · the journal trigger that could not see its own target · the logrotate `create` gap · the coverage marker that reported zero

**2026-09-17** — host-1/2/3 hardened to USG 213/3 and V1R6 171/9/9/5, byte-identical · Q20 (TPM) and Q21 (GRUB) answered.
